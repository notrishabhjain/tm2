<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this theme only needs to follow the system
         day/night setting and stay out of the way at launch.

         Note: there is no android:Theme.DeviceDefault.DayNight.NoActionBar -
         the DayNight variant has no NoActionBar child, so the action bar is
         turned off with the two window flags instead. -->
    <style name="Theme.TaskMind" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
