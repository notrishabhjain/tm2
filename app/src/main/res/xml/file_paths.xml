<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- The downloaded update APK, and diagnostic reports the user shares out.
         Nothing else in app storage is reachable through the provider. -->
    <cache-path name="updates" path="updates/" />
    <cache-path name="diagnostics" path="diagnostics/" />
</paths>
