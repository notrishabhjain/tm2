<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- API keys are keystore-bound and would not survive a restore anyway;
         backing up the ciphertext would only produce a confusing failure. -->
    <exclude domain="sharedpref" path="taskmind_secrets.xml" />
    <exclude domain="sharedpref" path="taskmind_secrets_plain.xml" />
</full-backup-content>
